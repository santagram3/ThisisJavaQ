package ch06;

public class q5 {
	
	// 5. 생성자에 대한 설명으로 틀린것은 ? 무엇? 1번 
	
	/*  
	 * 1. 객체를 생성하려면 생성자 호출이 필요한 것은 아니다 . // 필요한데 ? 이거네 
	 * 2. 생성자는 다른 생성자를 호출하기 위해 this()를 사용할 수 있다. // 그렇지 
	 * 3. 생성자가 선언되지 않으면 컴파일러가 기본 생성자를 추가한다.  // 맞지 
	 * 4. 외부에서 객체를 생성할 수 없도록 생성자에 private 접근제한자를 붙일 수 있다 // 싱글톤할때 했지 
	 * 
	 * 
	 * */

}
